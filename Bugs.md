# Bug Tracker

**Open: 6 | Fixed: 22**

---

## Open (June 2)

| #   | Severity   | Reported by | Description                                                                                                                                                                                                                    | Notes                                                                                                                                                              |
| --- | ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 25  | **High**   | Tobias      | Cannot join games from homepage lobby section or from lobby page join button - completely broken for all users                                                                                                                 | Root cause: `requireEmailVerified` middleware on `POST /games/:gid/players` blocks seed users whose `emailVerified` defaults to `false`. `LobbySection.jsx` also passes no `onJoin` prop to `LobbyCard` - homepage only has card click, no join button. |
| 26  | **High**   | Johan       | Spectating Top Games on homepage requires login - anonymous users get blocked when clicking into a game                                                                                                                        | Root cause: `/games/:id` route is nested inside `<ProtectedRoute>` in `App.jsx:37`. Fix: move it outside. |
| 27  | **Medium** | Johan       | Guest users can edit their profile and set a password, turning the account into a regular user without an email. Creating a new guest session then causes `E11000 duplicate key error index: email_1 dup key: { email: null }` | Fix discussed: disable Edit Profile for guest accounts. Seb believes the raw duplicate key error is already fixed; the root cause (guest editing profile) remains. |
| 28  | **Medium** | Johan       | Game History page (`/users/:username/games`) is broken                                                                                                                                                                         | Likely a frontend routing or data-fetching issue                                                                                                                   |
| 29  | **Medium** | Johan       | Guest join from lobby: clicking Join on a lobby game as a guest does nothing - only "Play as Guest" button on the game page works                                                                                              | Related to #25 - `requireEmailVerified` checks `req.user?.isGuest` from the JWT payload. If `isGuest` is not included in the token, guest users get blocked the same way. |
| 30  | **Low**    | Johan       | Something goes wrong with the timer when a game is left and rejoined - error related to time remaining                                                                                                                         | Needs reproduction steps to narrow down                                                                                                                            |

---

## Fixed

| #   | Severity     | Description                                                                                                                                     | What was changed                                                                                                                                                                                        |
| --- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Critical** | No "check" action - validator rejected it, no Check button in UI                                                                                | Added "check" to `BET_ACTIONS` in constants, validator `isIn` list, `placeBet` handler in service, and Check button in `GameBoard.jsx`                                                                  |
| 2   | **Critical** | Stack = buyIn (1 pt), players ran out after round 1; no bet cap                                                                                 | Fixed `createGame` to set `startingStack = buyIn * rounds`; added `roundBetCap = buyIn` enforcement in `placeBet`                                                                                       |
| 3   | **Security** | `getGameState` returned unsanitized hidden dice to all viewers                                                                                  | Changed controller to call `sanitizeGameForViewer(game, req.user?.id)` instead of returning raw game data                                                                                               |
| 4   | **High**     | Hold dice and reroll not wired - server always re-rolled all 5 dice                                                                             | `rollForPlayer` now accepts `heldIndexes`; reroll keeps held dice, re-rolls others with `rollDie()`; controller passes `req.body.heldIndexes`; `GameBoard` sends held indexes on roll                   |
| 5   | **Medium**   | Anonymous users could trigger bet/fold/raise socket events                                                                                      | Added `if (!socket.user?.id) return socket.emit("error", ...)` guard on all betting socket handlers in `game.socket.js`                                                                                 |
| 6   | **Low**      | Bet amount validator allowed 0                                                                                                                  | Changed `.isInt({ min: 0 })` to `.isInt({ min: 1 })` in `game.validator.js`                                                                                                                             |
| 7   | **Low**      | "revealing" phase defined in constants and `gameHelpers.js` but never set by the service                                                        | Removed "revealing" from constants and unused helper; service goes directly from "betting" to "round-ended"                                                                                             |
| 8   | **Critical** | `rollCount` missing from socket broadcast; timeout auto-roll missing rollCount; hardcoded 3; wrong error string; Bet/Raise label stuck on "Bet" | Added `rollCount` to `buildGameState`; set `rollCount: MAX_ROLLS_PER_TURN` on timeout result; extracted constant; fixed controller error string; Bet/Raise label now reads `currentBet`                 |
| 9   | **High**     | `MAX_ROLLS_PER_TURN` duplicated in frontend - server is source of truth                                                                         | Removed frontend constant; Roll button now shows "Reroll" without count and relies on `isMyTurn` to hide                                                                                                |
| 10  | **High**     | All-in player (stack=0) blocked betting round completion                                                                                        | Fixed `bettingRoundIsComplete` in `gameHelpers.js`: `contribution.amount === currentBet \|\| stackEntry.stack === 0`                                                                                    |
| 11  | **Medium**   | `hold-dice` socket event received `heldDiceIndexes` but did nothing - misleading dead code                                                      | Removed the handler entirely from `game.socket.js`                                                                                                                                                      |
| 12  | **Low**      | No validation for `heldIndexes` in roll request - could be invalid array or out-of-range values                                                 | Added validator in `game.validator.js` and wired it into `game.router.js` for the roll endpoint                                                                                                         |
| 13  | **High**     | `betAmount` not clamped - user could type negative or over-buyIn values                                                                         | Clamped in `onChange`: `Math.max(1, Math.min(serverState?.buyIn, val))` in `GameBoard.jsx`                                                                                                              |
| 14  | **Medium**   | `betAmount` not reset between rounds - stale value from last round stayed in the input                                                          | Reset to 1 when phase changes to "betting" in `GameBoard.jsx`                                                                                                                                           |
| 15  | **Low**      | Submit button always enabled when comment was empty                                                                                             | Added `disabled={!newComment.trim()}` to Post button in `Comments.jsx`                                                                                                                                  |
| 16  | **Critical** | Forfeit with 3+ players broken - `.find()` picked only one opponent instead of all remaining players                                            | Fixed `game.service.js` to find all remaining players and distribute pot/scores correctly                                                                                                               |
| 18  | **Medium**   | Socket bet/match/raise/fold events emitted same state to all - could leak hidden dice                                                           | Changed to personalized emit per socket in `game.socket.js`, same pattern as `emitPersonalizedState`                                                                                                    |
| 19  | **Critical** | `updateComment` had no owner/admin check - any logged-in user could edit any comment                                                            | Added author/admin check in `comment.controller.js` before calling service, matching the same pattern used by `deleteComment`                                                                           |
| 20  | **Low**      | CORS origin hardcoded to `localhost:5173` - would break in production                                                                           | Changed `backend/app.js` to use `process.env.FRONTEND_URL \|\| "http://localhost:5173"`                                                                                                                 |
| 24  | **High**     | Dice hold clicks broken - custom `die-click` event swallowed by shadow DOM (missing `composed: true`)                                           | Removed event from `game-die.js`; `game-board.js` now listens for native `click` on shadowRoot and uses `closest("game-die")` to find the clicked die; roll also sets state from REST response directly |
| 17  | **High**     | Result object created in betting phase missing `rollCount`, `holds`, `bets` fields                                                              | Added fields explicitly in `game.service.js` as part of the multiplayer forfeit fix                                                                                                                     |
| 23  | **Medium**   | No handler for `game-deleted` socket event - players stayed on a deleted game with no feedback                                                  | Added `socket.on("game-deleted", () => onGameDeleted?.())` in `GameBoard.jsx`; `GamePage` passes `onGameDeleted={() => navigate("/lobby")}`                                                             |
