# Bug Tracker

**Open: 2 | Fixed: 46**

---

## Open

| #   | Severity   | Reported by  | Description                                                                                                                                                                                                                                                                                                                                                  | Notes                                                                                                                                                                                                             |
| --- | ---------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 30  | **Low**    | Johan        | Something goes wrong with the timer when a game is left and rejoined - error related to time remaining                                                                                                                                                                                                                                                       | Needs reproduction steps to narrow down                                                                                                                                                                           |
| 47  | **Low**    | Johan        | Game board info bar (economy bar + phase bar) layout needs polish - current bet placement, bet input height not matching button height, overall visual hierarchy could be cleaner                                                                                                                                                                              | Consider redesigning the info bar as a single cohesive row or card; bet input should match button height; current bet could move into phase bar or get its own styled chip                                        |

---

## Fixed (June 2)

| #   | Severity   | Description                                                                                                                      | What was changed                                                                                                                                                                                            |
| --- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 25  | **High**   | Cannot join games - broken for all users                                                                                         | Added `emailVerified: true` and `points` to seed users in `users.json`; added `numPlayers` and `buyIn` to all seed games; added `playerStacks` resolution in `seed.js`                                      |
| 26  | **High**   | Spectating requires login - anonymous users blocked when clicking into a game                                                    | Moved `/games/:id` outside `<ProtectedRoute>` in `App.jsx` - accidentally bundled in with protected routes in commit `c8b6689`                                                                              |
| 27  | **Medium** | Guest users can edit their profile causing `E11000 duplicate key error` on null email when creating a new guest                  | Added `!profile.isGuest` guard to Edit Profile button in `UserProfilePage.jsx`; added `User.syncIndexes()` in `app.js` to drop stale email index on startup                                                 |
| 28  | **Medium** | Game History page (`/users/:username/games`) shows nothing                                                                       | Backend returns `{ games, total, hasMore }` but frontend set state to the whole object - fixed to destructure `data.games`                                                                                  |
| 29  | **Medium** | Guest join from homepage lobby does nothing - no "Play as Guest" button                                                          | Added `handleGuestJoin` to `LobbySection.jsx` and wired `onGuestJoin` prop to `LobbyCard` for `allowAnonymous` games when not logged in                                                                     |
| 31  | **High**   | Points not refunded when leaving a waiting game                                                                                  | Added point refund (`$inc: { points: startingStack }`) and `playerStacks` cleanup in `leaveGame` waiting path in `game.service.js`                                                                          |
| 32  | **Medium** | `PATCH /users/:username/preferences` had no auth guard - any user could overwrite anyone's preferences                           | Added `requireSelfOrAdmin` to the route in `user.router.js`                                                                                                                                                 |
| 33  | **Medium** | `joinGame` and `joinTournament` used `req.body.player` - logged-in user could join on behalf of any player ID                    | Changed both controllers to use `req.user.id` instead                                                                                                                                                       |
| 34  | **Low**    | Typo in `createGame` error: "You don't not have enough points" (double negation)                                                 | Fixed to "You do not have enough points" in `game.service.js`                                                                                                                                               |
| 42  | **Medium** | Bet amount input clamped on every keystroke - impossible to type multi-digit numbers like "10" on mobile                         | Changed `onChange` to allow empty string while typing; added `onBlur` to clamp to 1 if empty; `handleBet` uses `Number(betAmount)` when sending in `GameBoard.jsx`                                          |
| 38  | **High**   | Forfeiting player's remaining stack disappeared - not returned to remaining player or pot                                        | In `leaveGame` forfeit path, added `game.pot += forfeiterStack.stack` before zeroing it so `splitPot` distributes the full pot (forfeited stack + any round bets) to remaining players in `game.service.js` |
| 35  | **Low**    | After email verification, user stays on `/verify-email` - no automatic redirect                                                  | Added `useEffect` that fires `setTimeout(() => navigate("/"), 2000)` when `verified` becomes true in `EmailVerificationPage.jsx` (Tobias)                                                                   |
| 36  | **Low**    | Double verification code sent on registration - `register()` called `createEmailVerification()` AND the page auto-sends on mount | Removed `createEmailVerification` call from `register()` in `auth.service.js` (Tobias)                                                                                                                      |
| 37  | **Low**    | `window.confirm()` used for forfeit - native browser dialog is unstyled                                                          | Replaced with in-game overlay: `confirmLeave` state, "Confirm" / "Cancel" buttons in `GamePage.jsx` + `GamePage.module.css` (Tobias)                                                                        |
| 39  | **Medium** | No winner overlay shown after forfeit in multiplayer - game finishes with no announcement                                        | Changed overlay condition from `game.result?.winner` to `game.status === "finished"`; shows "Game over" when no winner (Tobias)                                                                             |
| 41  | **Low**    | No way to click a player's profile from inside a game - player cards not links                                                   | Wrapped player card content in `<Link to={/users/${player.username}}>` with `pLink` CSS class in `PlayersSection.jsx` (Tobias)                                                                              |
| 45  | **High**   | Game stuck in rolling phase - timeout mid-reroll left rollCount below MAX; everyoneFinished never resolved; turn cycled back to already-finished players | Added `else` branch in `handleTimeout` setting `timedOutResult.rollCount = MAX_ROLLS_PER_TURN`; replaced `everyoneRolled` with `everyoneFinished` check (Johan) |
| 46  | **Medium** | Email verification code only appeared in server console - awkward for demo | Created `backend/services/email.service.js` with nodemailer Gmail SMTP; `auth.service.js` now calls `sendVerificationEmail(user, code)`; falls back to `console.log` if env vars missing |
| 48  | **High**   | Bet contributions not persisted between turns - bets placed in earlier turns were lost, pot calculations broke | `getContribution` in `gameHelpers.js` pushed a plain object then used a local variable reference, not the Mongoose subdocument proxy; rewrote to re-reference the pushed element; added `game.markModified("bettingState")` before save in `game.service.js` (Tobias) |
| 44  | **Medium** | Bet input `onBlur` only checked lower bound - typing above `buyIn` and blurring sent invalid amount                              | Added upper bound check: `else if (val > buyIn) setBetAmount(buyIn)` in `onBlur` in `GameBoard.jsx` |
| 40  | **Low**    | Held dice still shown as held after final roll - hold state meaningless when no reroll possible                                  | In `GameBoard.jsx` dice sync effect: `held: rollsUsed < MAX_ROLLS_PER_TURN && heldDice.has(i)`; `handleHoldDie` returns early when `rollsUsed >= MAX_ROLLS_PER_TURN`; added `rollsUsed` to effect deps      |
| 43  | **Medium** | Comment section not visible on mobile - sidebar collapses to zero height in single-column layout                                 | Added `min-height` and mobile breakpoint fix in `GamePage.module.css`; limited comment length to 200 chars, added max-height scroll to comment list (Tobias) |

---

## Fixed

| #   | Severity     | Description                                                                                                                                     | What was changed                                                                                                                                                                                        |
| --- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Critical** | No "check" action - validator rejected it, no Check button in UI                                                                                | Added "check" to `BET_ACTIONS` in constants, validator `isIn` list, `placeBet` handler in service, and Check button in `GameBoard.jsx`                                                                  |
| 2   | **Critical** | Stack = buyIn (1 pt), players ran out after round 1; no bet cap                                                                                 | Fixed `createGame` to set `startingStack = buyIn * rounds`; added `roundBetCap = buyIn` enforcement in `placeBet`                                                                                       |
| 3   | **Security** | `getGameState` returned unsanitized hidden dice to all viewers                                                                                  | Changed controller to call `sanitizeGameForViewer(game, req.user?.id)` instead of returning raw game data                                                                                               |
| 4   | **High**     | Hold dice and reroll not wired - server always re-rolled all 5 dice                                                                             | `rollForPlayer` now accepts `heldIndexes`; reroll keeps held dice, re-rolls others with `rollDie()`; controller passes `req.body.heldIndexes`; `GameBoard` sends held indexes on roll                   |
| 5   | **Medium**   | Anonymous users could trigger bet/fold/raise socket events                                                                                      | Added `if (!socket.user?.id) return socket.emit("error", ...)` guard on all betting socket handlers in `game.socket.js`                                                                                 |
| 6   | **Low**      | Bet amount validator allowed 0                                                                                                                  | Changed `.isInt({ min: 0 })` to `.isInt({ min: 1 })` in `game.validator.js`                                                                                                                             |
| 7   | **Low**      | "revealing" phase defined in constants and `gameHelpers.js` but never set by the service                                                        | Removed "revealing" from constants and unused helper; service goes directly from "betting" to "round-ended"                                                                                             |
| 8   | **Critical** | `rollCount` missing from socket broadcast; timeout auto-roll missing rollCount; hardcoded 3; wrong error string; Bet/Raise label stuck on "Bet" | Added `rollCount` to `buildGameState`; set `rollCount: MAX_ROLLS_PER_TURN` on timeout result; extracted constant; fixed controller error string; Bet/Raise label now reads `currentBet`                 |
| 9   | **High**     | `MAX_ROLLS_PER_TURN` duplicated in frontend - server is source of truth                                                                         | Removed frontend constant; Roll button now shows "Reroll" without count and relies on `isMyTurn` to hide                                                                                                |
| 10  | **High**     | All-in player (stack=0) blocked betting round completion                                                                                        | Fixed `bettingRoundIsComplete` in `gameHelpers.js`: `contribution.amount === currentBet \|\| stackEntry.stack === 0`                                                                                    |
| 11  | **Medium**   | `hold-dice` socket event received `heldDiceIndexes` but did nothing - misleading dead code                                                      | Removed the handler entirely from `game.socket.js`                                                                                                                                                      |
| 12  | **Low**      | No validation for `heldIndexes` in roll request - could be invalid array or out-of-range values                                                 | Added validator in `game.validator.js` and wired it into `game.router.js` for the roll endpoint                                                                                                         |
| 13  | **High**     | `betAmount` not clamped - user could type negative or over-buyIn values                                                                         | Clamped in `onChange`: `Math.max(1, Math.min(serverState?.buyIn, val))` in `GameBoard.jsx`                                                                                                              |
| 14  | **Medium**   | `betAmount` not reset between rounds - stale value from last round stayed in the input                                                          | Reset to 1 when phase changes to "betting" in `GameBoard.jsx`                                                                                                                                           |
| 15  | **Low**      | Submit button always enabled when comment was empty                                                                                             | Added `disabled={!newComment.trim()}` to Post button in `Comments.jsx`                                                                                                                                  |
| 16  | **Critical** | Forfeit with 3+ players broken - `.find()` picked only one opponent instead of all remaining players                                            | Fixed `game.service.js` to find all remaining players and distribute pot/scores correctly                                                                                                               |
| 18  | **Medium**   | Socket bet/match/raise/fold events emitted same state to all - could leak hidden dice                                                           | Changed to personalized emit per socket in `game.socket.js`, same pattern as `emitPersonalizedState`                                                                                                    |
| 19  | **Critical** | `updateComment` had no owner/admin check - any logged-in user could edit any comment                                                            | Added author/admin check in `comment.controller.js` before calling service, matching the same pattern used by `deleteComment`                                                                           |
| 20  | **Low**      | CORS origin hardcoded to `localhost:5173` - would break in production                                                                           | Changed `backend/app.js` to use `process.env.FRONTEND_URL \|\| "http://localhost:5173"`                                                                                                                 |
| 24  | **High**     | Dice hold clicks broken - custom `die-click` event swallowed by shadow DOM (missing `composed: true`)                                           | Removed event from `game-die.js`; `game-board.js` now listens for native `click` on shadowRoot and uses `closest("game-die")` to find the clicked die; roll also sets state from REST response directly |
| 17  | **High**     | Result object created in betting phase missing `rollCount`, `holds`, `bets` fields                                                              | Added fields explicitly in `game.service.js` as part of the multiplayer forfeit fix                                                                                                                     |
| 23  | **Medium**   | No handler for `game-deleted` socket event - players stayed on a deleted game with no feedback                                                  | Added `socket.on("game-deleted", () => onGameDeleted?.())` in `GameBoard.jsx`; `GamePage` passes `onGameDeleted={() => navigate("/lobby")}`                                                             |
